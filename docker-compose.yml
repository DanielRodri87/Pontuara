services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pontuara-backend
    env_file:
      - ./backend/.env
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "7000:8000" # <-- O host acessa na 7000, e redireciona para a 8000 do container
    volumes:
      - ./backend:/app
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: pontuara-frontend
    depends_on:
      - backend
    environment:
      WATCHPACK_POLLING: "true"
      NEXT_TELEMETRY_DISABLED: "1"
      NEXT_PUBLIC_API_URL: http://localhost:7000 # <-- Apontando para o backend na 7000
    command: npm run dev
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    restart: unless-stopped